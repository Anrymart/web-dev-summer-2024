/**
 * https://javascript.info/introduction-browser-events
 *
 *
 * Mouse events:
 * - click
 * - contextmenu
 * - mouseover / mouseout
 * - mousedown / mouseup
 * - mousemove
 *
 * Keyboard events:
 * - keydown and keyup
 *
 * Document events:
 * - DOMContentLoaded - when the HTML is loaded and processed, DOM is fully built
 *
 *
 *
 * There are 3 ways to assign event handlers:
 *
 * HTML attribute: onclick="...".
 * DOM property: elem.onclick = function.
 * Methods: elem.addEventListener(event, handler[, options]) to add, removeEventListener to remove.
 *
 *
 * Event propagation
 *
 *
 * Task:
 * Build a simple calculator which can add and subtract 2 numbers
 */
